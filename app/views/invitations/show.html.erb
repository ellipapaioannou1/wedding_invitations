<p>
  <strong><h4>Name :</h4></strong>
  <%= @invitation.name  %>
</p>

<p>
  <strong><h4>Persons :</h4></strong>
  <%= @invitation.persons_count %>
  
</p>

<%= link_to 'Edit', edit_invitation_path(@invitation), class:"btn btn-light btn-lg" %> |
<%= link_to 'Back', invitations_path, class:"btn btn-light btn-lg" %>



<style type="text/css">
table {
  border-width: 0;
  border-style: none;
  border-color: #0000ff;
  border-collapse: collapse;
}
td {
  border-width: 0;
  border-style: none;
  border-color: #0000ff;
  border-collapse: collapse;
  padding: 0;
  margin: 0;
  width: 10px;
  height: 10px;
}
td.black { background-color: #000; }
td.white { background-color: #fff; }
</style>

<%= raw @qr.as_html %>